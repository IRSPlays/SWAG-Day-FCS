# Environment

- Windows machine (paths like `C:\Users\haziq\...`); default shell is cmd — use cmd idioms (`set VAR=`, `robocopy /E /MOVE`, `2>nul`, `&` chaining) and PowerShell one-liners for advanced ops (web requests, System.Drawing pixel sampling). Confidence: 0.8
- Global npm config `omit=dev` is set on this machine, so npm silently skips devDependencies (and `npm ls` reports an empty tree even when packages are on disk) — install with `npm install --include=dev` or dev-dependent builds fail. Confidence: 0.8
- `NODE_ENV=production` is set in the shell environment; it breaks Turbopack/Next.js builds ("Cannot find module '@tailwindcss/postcss'") even when dev deps are physically installed — unset it per-command: `set NODE_ENV= & npm run build`. Confidence: 0.8
- No browser automation is installed (agent-browser absent) — verify UI changes headlessly: run the build, boot the dev server on port 3000 in the background, then fetch served HTML/CSS with PowerShell `Invoke-WebRequest` and assert on content; stop the server afterwards, and free port 3000 (`kill_shell` by port) if an orphaned node process still holds it. Confidence: 0.7
- `next/font/google` build-time downloads fail for every family ("Failed to download Anton/IBM Plex Mono/Instrument Serif/Space Grotesk from Google Fonts. Using a fallback font instead") — but the machine's network itself is FINE: curl.exe with a browser User-Agent reaches fonts.googleapis.com and downloads the woff2s successfully (the block is specific to Next's internal font fetcher). Fix pattern: curl the Google Fonts CSS + latin woff2 files into the repo and self-host via `next/font/local` (done for SWAG Day FCS). Test connectivity with curl before assuming the network is down. Confidence: 0.85
- Projects must live under `C:\Projects\` — not OneDrive-synced paths (OneDrive file-sync locks break installs/builds). Confidence: 0.8
- Wants the server to self-heal port conflicts instead of failing: if port 3000 is taken, walk up to the next free port (3000 → 3001 → ...) with a bounded retry on `EADDRINUSE` and log the actual bound port ("Can u make it so that if port 3000 is not available just use port 3001"). Don't ask them to kill stale processes — just walk the port. Safe to do blindly because all clients derive the hub URL from `window.location`. Confidence: 0.7
- Avoid if/else blocks in one-liners — write explicit statements instead ("PowerShell 5.1 doesn't support ternaries — rewriting"). Confidence: 0.8
- The stage machine has a dedicated GPU, and the lyric-engine lag root cause was Chromium defaulting to the INTEGRATED GPU — once the browser ran on the dGPU, the dual-monitor 1080p engine was smooth ("previously it was running on the igpu now on my dedicated gpu... its not laggy at all"). If lag ever returns (driver/Windows update resetting preferences), pin chrome.exe to High performance via Windows Settings → Display → Graphics before reaching for cheaper effects. Confidence: 0.8